# GenerativeAI-Lab-Assignment.
A simple feedforward neural network implemented from scratch using Python and NumPy to classify Iris flowers into three species. The project demonstrates data preprocessing, ReLU and Softmax activation, forward propagation, backpropagation, gradient descent, and model evaluation without using deep-learning frameworks.
